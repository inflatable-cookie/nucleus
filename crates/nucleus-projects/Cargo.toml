[package]
name = "nucleus-projects"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
nucleus-core = { path = "../nucleus-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
