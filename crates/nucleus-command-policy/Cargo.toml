[package]
name = "nucleus-command-policy"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
nucleus-core = { path = "../nucleus-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
