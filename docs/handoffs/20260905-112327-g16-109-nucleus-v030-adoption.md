---
title: g16.109 Nucleus v0.3.0 adoption worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: awaiting-review
owner: Tom
created: 2026-09-05
updated: 2026-09-05
handoff_path: /Users/tom/.paseo/worktrees/17ggapja/g16-109-nucleus-v030-adoption/docs/handoffs/20260905-112327-g16-109-nucleus-v030-adoption.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, poodle-adoption, g16]
---

## What This Thread Was Doing

This is the Nucleus tier-2 lane of Poodle's `g16.109` consumer adoption wave.
Longhorn already merged public Poodle `0.3.0` at
`168ecc72be5d8643afe6e1246f080cc1c07701ab`. The job is to pin Nucleus Desktop
to the same public `@inflatable-cookie/poodle-core` and
`@inflatable-cookie/poodle-svelte` `0.3.0`, absorb the documented markdown
import move, prove one registry identity, and open one PR against Nucleus
`main`.

## Why It Matters

Nucleus is a current Longhorn-backed app. Until this pin moves, desktop still
installs Poodle `0.2.2` while the sibling Longhorn adapter peer is `0.3.0`.
That split is the exact duplicate-identity case the wave forbids.

## Current State

- **Repository:** `git@github.com:inflatable-cookie/nucleus.git`
- **Planning branch:** `main`
- **Planning base commit:** `e8de352c3b39a28ec97b425eeed674ecacccac1f`
- **Pushed main verification:** worker branch reset to `origin/main` at that
  commit before edits
- **Planning checkout:** primary checkout at `/Users/tom/Dev/projects/nucleus`
  is on `main`; do not edit it
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight
- **Planning artifacts included at the base:** Poodle card
  `/Users/tom/Dev/projects/poodle/docs/roadmaps/g16/109-v030-consumer-adoption-wave.md`
  on Poodle `main`
- **Worker branch:** `worker/g16-109-nucleus-v030-adoption`
- **Worker worktree:**
  `/Users/tom/.paseo/worktrees/17ggapja/g16-109-nucleus-v030-adoption`
- **Worktree creation command:** launcher-provided Paseo worktree; do not
  create another
- **Required sibling worktree links:** `longhorn` →
  `/Users/tom/Dev/projects/longhorn`, available as `../longhorn` from this
  worktree. Verified: symlink already resolved to that source at Longhorn
  `168ecc72be5d8643afe6e1246f080cc1c07701ab`
- **Active spec lane:** none; consumer adoption of a published release
- **Roadmap milestone:** Poodle `g16.109` tier 2, Nucleus row
- **Ready cards, in order:** Poodle
  `/Users/tom/Dev/projects/poodle/docs/roadmaps/g16/109-v030-consumer-adoption-wave.md`
- **Allowed runway:** Nucleus Poodle declarations, desktop lock, bounded
  compatibility fallout, `PAPERCUTS.md` append, this handoff
- **Remaining card budget:** this one Nucleus lane
- **Coordinator agent ID:** manual / operator-dispatched worker
- **Delivery route:** one PR against Nucleus `main`; merge stays with the
  Poodle coordinator
- **Dispatch topology:** Poodle `g16.109` tier 2 Longhorn-app lanes also
  include Bovine Accelerator Desktop, Figmatic, Finch, Jetstream, Loophole
- **Parallel safety check:** no writes outside this Nucleus worktree; do not
  edit Poodle or Longhorn
- **Surfaces this lane owns:** `apps/desktop/package.json`;
  `apps/desktop/bun.lock`; markdown-family import fallout in desktop source;
  `PAPERCUTS.md` append; this handoff
- **Integration ownership:** Poodle wave closeout (`g16/README.md`,
  generation index, README adoption count) stays with the Poodle coordinator
- **Canonical refs:** Nucleus `AGENTS.md`; Poodle
  `docs/release-notes/0.3.0.md`; Poodle tag `v0.3.0` at `85609d941`; Longhorn
  merge `168ecc72be5d8643afe6e1246f080cc1c07701ab`
- **Review oracle:** Poodle card `g16.109` table (one identity; breaks
  absorbed not shimmed; board green; papercuts closed honestly)
- **Model capability profile:** ordinary implementation
- **Worker provider/model identity:** Grok 4.6
- **Frontier-worker justification:** none
- **Tool/runtime restrictions:** public registry only for Poodle; keep the
  three intended Longhorn `file:` packages; no sibling Poodle path; no
  desktop/native/windowed proofs; no workflow edits; no merge
- **Required validation:** `effigy check:longhorn-consumer`,
  `effigy desktop:check`, `effigy desktop:build`, `effigy desktop:test`,
  lock grep / `effigy --json deps status bun`, `git diff --check`
- **PR base/head:** `e8de352c3b39a28ec97b425eeed674ecacccac1f` (`main`) ←
  `worker/g16-109-nucleus-v030-adoption` (head filled after this commit)
- **PR URL:** pending
- **Review state:** awaiting independent exact-head review
- **Merge path:** orchestrator after accepted review of the current head and
  passing required checks

## Boundaries

- **In scope:** exact public `0.3.0` pins wherever
  `@inflatable-cookie/poodle-core` and `@inflatable-cookie/poodle-svelte` are
  declared; lock regeneration limited to Poodle and the markdown optional
  peer the bump now requires; `/markdown` import move; honest papercut
  append; one PR
- **Out of scope:** Underlay refs (Nucleus declares none); HistoryCenter v3
  (Nucleus has no HistoryCenter caller); Poodle or Longhorn edits; replacing
  Longhorn `file:` deps; desktop launch or native proof; workflow edits;
  merge
- **Outcome shape:** complete pin-and-proof lane. Open a reviewable PR; do
  not merge
- Work only in the launcher worktree. Do not edit the primary checkout
- Stop if the bump exposes a Nucleus defect that is not compatibility
  fallout, if lock churn is unrelated, or if public registry resolution fails

## Important Context

- **Planning lineage:** Poodle `g16.097` published `0.3.0`. `g16.109` is the
  consumer wave. Nucleus previously pinned public `0.2.2` (g15.065 / Nucleus
  PR 1). Longhorn PR 21 is the foundation for this lane
- **Why these cards are ready:** npm `latest` is `0.3.0`; Longhorn main is
  at the merged pin; the sibling checkout is that commit
- **Decisions and preferences:** resolve Poodle from the public registry
  only. Use this repo's Bun `1.3.14`. Keep local Longhorn packages. Inspected
  every manifest: only `apps/desktop/package.json` declares Poodle; no Cargo
  Poodle pin; no Underlay `#v0.9.7`
- **Open tensions:** Nucleus `PAPERCUTS.md` has no open entry that `0.3.0`
  actually fixes. Do not close the stale `[x]` Longhorn-consumer note still
  sitting under Open. Record the two hurdles this lane hit instead
- **Compatibility fallout absorbed:** `AgentPlan` and `AgentTranscript` now
  import from `@inflatable-cookie/poodle-svelte/markdown`. `marked@18.0.9` is
  a direct desktop dependency because `0.3.0` moved it from a Svelte package
  dependency to an optional peer and the markdown barrel still imports it.
  No shim, alias, or re-export of removed root names
- **Report after:** one PR with lock/dependency evidence and the board
- **Report to:** the operator / owning coordinator. Do not merge

## Suggested Next Move

Independent exact-head review of the PR against the card oracle, this
handoff, the lock grep, and the validation listed below. Do not merge from
this worker.

## Completion Protocol

### Before you start

1. This handoff's `worker_mode: implementation` and
   `dispatch_authority: orchestrator` activate worker mode. Before broad reads,
   run `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. If the current root is a registered worktree, its status is empty, and its
   branch is not `main`, accept it as the launcher-provided worktree. Record
   its actual root/branch; do not create another worktree.
3. From the selected worktree, record this handoff's repository-relative path
   (`docs/handoffs/20260905-112327-g16-109-nucleus-v030-adoption.md`). Run
   `GIT_SSH_COMMAND="ssh -o ConnectTimeout=10 -o BatchMode=yes" git fetch origin`.
   Confirm the planning base is an ancestor of `HEAD`. After this file is
   committed, the tracked `HEAD` copy is canonical.
4. Verify the Longhorn sibling link as named above. Reuse only that exact
   symlink; stop on a missing source or conflicting destination.
5. Read the Poodle card, Nucleus `AGENTS.md`, and the desktop manifest.
6. Run cheap orientation (`effigy tasks`) and record it.

### While you work

- Keep commits aligned with one adoption chunk, not model turns.
- Stop before PR if a contract is missing, intent is ambiguous, or validation
  changes the plan.
- Do not invent architecture or shim removed Poodle exports.

### When the assigned runway is complete

Validation actually run on this worker, all green:

- Bun `1.3.14`
- `effigy desktop:check` — 0 errors, 0 warnings
- `effigy desktop:build` — pass after adding `marked@18.0.9`
- `effigy desktop:test` — 71 bun tests pass, 38 vitest tests pass
- `effigy check:longhorn-consumer` — pass; Longhorn
  `168ecc72be5d8643afe6e1246f080cc1c07701ab`; Poodle `0.3.0` from
  `npm-registry`; one Poodle runtime
- `effigy --json deps status bun` — healthy; the only `file:` sources are
  the three intended Longhorn packages
- Lock grep: one `@inflatable-cookie/poodle-core@0.3.0` and one
  `@inflatable-cookie/poodle-svelte@0.3.0`; no `file:` / `link:` Poodle
  source; no remaining Poodle `0.2.2`. Integrity
  `sha512-/l1yl11lXFq3Th7DnXKYBtAuvUJRNfhRLluqs4HTov3MPEnN6htgDw/FFV0XZrKdEq9/cgs3M+U5lFRKP/aIPQ==`
  matches Longhorn's lock
- `git diff --check` — pass
- Not run: `desktop:dev`, `desktop:proof`, windowed selectors, workflow
  edits, full `effigy qa`, merge

### Review and merge path

Independent review of the exact PR head. Merge belongs to the orchestrator
after accepted review and passing checks.

- **Closeout refs:** this handoff; Poodle `g16.109` Nucleus row. Poodle
  generation-index live pointer is not this lane's closeout.

### Handoff closeout

Leave the PR honest: Nucleus has no Underlay git ref to move, no
HistoryCenter caller, and no open papercut that `0.3.0` itself closes.
The two new papercuts are observations from this bump, not extra product
work.
