# Floating Agent Chat Composer Closeout

Date: 2026-07-14

Agent Chat now uses a floating composer with real per-turn model and reasoning
overrides. The provider catalog supplies the compact selectors; the server
validates and records the effective route. Operator tuning tightened alignment,
font size, send control, placeholder contrast, and menu density.

Desktop checks passed during the lane. The operator accepted the current shape.
