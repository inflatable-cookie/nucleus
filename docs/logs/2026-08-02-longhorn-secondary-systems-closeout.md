# Longhorn Secondary Systems Closeout

Date: 2026-08-02
Cards: g05.038-g05.055

## Result

The executable secondary-system runway is complete. Nucleus now composes:

- a 27-command semantic catalogue, sparse keymap, palette, and Keybindings page
- one bounded cross-panel operation catalogue
- one durable, redacted attention ledger with restrained toasts
- explicit backup capture, inventory, retention, host-selected export, Storage,
  and Backups pages
- one consumer-native local bridge over existing typed Nucleus control envelopes

Nucleus retains product meaning, authorization, DTOs, redaction, persistence,
and consequences. Longhorn retains the generic mechanisms. Hosted Surfaces,
generic history, isolated/backing content, native notifications, and offline
queues remain non-adoptions.

## Conformance

The consumer verifier now covers every adopted renderer package and Rust crate
at Longhorn commit `3032545b3284d3af7f976a88827bb8c8f5c94513`. Selected
sources are clean. Produced renderer archives install in an isolated consumer
with one Svelte and one Poodle runtime. Forbidden Surface and history packages
remain absent. Focused lifecycle evidence is present for settings, commands,
operations, notifications, exact-snapshot backup export, and consumer-native
local bridge invocation.

Doctor returned to the admitted 26 oversized-file error baseline after the two
new test harnesses were split. Generated-in-source findings remain warning-only
pre-existing debt.

## Paused Edges

- restore needs grouped failure-atomic custom-adapter publication from Longhorn
  plus Nucleus boot-time quiescence and a durable restart handoff
- remote transport needs a promoted pairing/session contract and topology
- secondary windows need an operator-selected product workflow
- final g05 product acceptance remains an operator checkpoint

No paused edge is advertised as a capability or production claim.
